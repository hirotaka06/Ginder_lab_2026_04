title = (
    "Safecast 空間線量率（μSv/h）ボロノイ図\n"
    "（福島周辺・半径100km / Safecast API 日次集計）"
)
colorbar_label = "空間線量率（μSv/h）"
cmap_name = "safecast"
reverse_colors = False

# 線量率は値域が広く（0.03〜数十μSv/h）、対数スケールで視認性が良くなる。
# vmin（最小閾値）は 0 より大きい必要がある（log(0)が定義できないため）。
norm_type = "log"

# 観測点が密集すると放射状の細いセルが多発し、白い縁取りがノイズに見えるので OFF。
show_cell_borders = False

# Safecast 公式マップの凡例にならった対数等比のしきい値。
# 各しきい値が「カラーバーの目盛り＝凡例ラベル」になる。
thresholds = [
    (0.03,  "0.03 µSv/h（背景レベル）"),
    (0.05,  "0.05"),
    (0.08,  "0.08"),
    (0.16,  "0.16"),
    (0.31,  "0.31"),
    (0.83,  "0.83"),
    (2.00,  "2.00"),
    (3.99,  "3.99"),
    (10.09, "10.09"),
    (65.54, "65.54 µSv/h（極めて高い）"),
]
